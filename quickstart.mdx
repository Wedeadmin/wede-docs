---
title: "Quickstart"
description: "Send your first event in under 5 minutes"
icon: 'rocket'
---

## Before you start

You need a Wede account and an API key. [Sign up at app.wede.pt](https://app.wede.pt/register) or contact [geral@wede.pt](mailto:geral@wede.pt) to get access.

Your API key looks like this: `wede_live_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX`

---

## Step 1 — Send your first event

Choose the example that matches your industry:

<Tabs>
  <Tab title="Healthcare">
```bash
curl -X POST https://api.wede.pt/v1/events \
  -H "x-wede-api-key: wede_live_YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "EMERGENCY_DISPATCH",
    "priority": "critical",
    "vertical": "healthcare",
    "idempotency_key": "hc-evt-001",
    "payload": {
      "unit": "AMB-PT-07",
      "patient_id": "PT-001",
      "location": "Hospital Santa Maria, Lisboa",
      "condition": "cardiac_arrest"
    }
  }'
```
  </Tab>
  <Tab title="Banking">
```bash
curl -X POST https://api.wede.pt/v1/events \
  -H "x-wede-api-key: wede_live_YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "FRAUD_ALERT",
    "priority": "high",
    "vertical": "banking",
    "idempotency_key": "bk-evt-001",
    "payload": {
      "terminal_id": "ATM-NG-042",
      "account_ref": "****4521",
      "amount": 150000,
      "currency": "NGN"
    }
  }'
```
  </Tab>
  <Tab title="Logistics">
```bash
curl -X POST https://api.wede.pt/v1/events \
  -H "x-wede-api-key: wede_live_YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "DELIVERY_STATUS",
    "priority": "normal",
    "vertical": "delivery",
    "idempotency_key": "lg-evt-001",
    "payload": {
      "driver_id": "DRV-KE-019",
      "order_id": "ORD-29841",
      "status": "arrived",
      "location": "Westlands, Nairobi"
    }
  }'
```
  </Tab>
  <Tab title="Telecom">
```bash
curl -X POST https://api.wede.pt/v1/events \
  -H "x-wede-api-key: wede_live_YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "NETWORK_ALERT",
    "priority": "high",
    "vertical": "telecom",
    "idempotency_key": "tc-evt-001",
    "payload": {
      "site_id": "BTS-AE-112",
      "alert_type": "power_failure",
      "region": "Dubai — Al Barsha"
    }
  }'
```
  </Tab>
  <Tab title="Government">
```bash
curl -X POST https://api.wede.pt/v1/events \
  -H "x-wede-api-key: wede_live_YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "CIVIL_ALERT",
    "priority": "critical",
    "vertical": "gov",
    "idempotency_key": "gv-evt-001",
    "payload": {
      "zone": "zona_norte_maputo",
      "alert_type": "evacuation",
      "population_affected": 12400
    }
  }'
```
  </Tab>
</Tabs>

You will receive a response confirming the event was accepted:

```json
{
  "event_id": "0ee3dfbe-1234-abcd-5678-ef90ab12cd34",
  "status": "pending",
  "channel_selected": "rest_full",
  "created_at": "2026-05-19T10:00:00Z"
}
```

---

## Step 2 — Check your zone connectivity

Wede monitors connectivity per operational zone. Check the current state:

```bash
curl https://api.wede.pt/v1/tenant/zones \
  -H "x-wede-api-key: wede_live_YOUR_KEY"
```

---

## Step 3 — Set up a webhook

Receive real-time delivery confirmations in your system:

```bash
curl -X POST https://api.wede.pt/v1/webhooks \
  -H "x-wede-api-key: wede_live_YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://your-system.com/wede-webhook",
    "events": ["event.delivered", "event.failed", "zone.state_changed"]
  }'
```

---

## What happens next

When you send an event, Wede:

1. Accepts the event and assigns a unique ID
2. Checks connectivity state for the relevant zone
3. Routes delivery through the best available channel
4. If the primary channel fails, falls back automatically
5. Confirms delivery and notifies your webhook

Your integration does not change — Wede handles routing transparently.

---

## Next steps

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/authentication">
    API keys, JWT tokens, and security best practices
  </Card>
  <Card title="Offline-First Architecture" icon="signal-bars-slash" href="/concepts/offline-first">
    How Wede keeps your operations running without internet
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/introduction">
    Full API documentation with examples
  </Card>
  <Card title="Verticals" icon="buildings" href="/concepts/verticals">
    Healthcare, banking, logistics, telecom, emergency and government
  </Card>
</CardGroup>
