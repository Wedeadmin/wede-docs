---
title: Offline-First Architecture
description: How Wede ensures operational continuity without internet
---

## What Offline-First Means

Most platforms assume internet connectivity. They are built for the best case. When connectivity fails, they fail too.

Wede is built for the worst case. Every feature, every integration, every delivery guarantee is designed to work without internet — and to work better when internet is available.

This is what offline-first means: not a fallback mode, but the default architecture.

## Delivery Channels

Wede maintains multiple delivery channels for every operational zone. When the primary channel degrades, the next channel activates automatically.

The channel hierarchy is managed by Wede based on zone configuration and real-time connectivity monitoring. Your integration does not change — Wede routes transparently.

## Connectivity States

Each zone has an independent connectivity state:

| State | Meaning |
|-------|---------|
| **Online** | Full connectivity, primary channel active |
| **Degraded** | Reduced connectivity, optimised routing active |
| **SMS Only** | Internet unavailable, alternative channels active |
| **Offline** | All external channels unavailable, local continuity active |

Zone states are monitored in real-time and visible in your [dashboard](https://app.wede.pt/dashboard/zones).

## Data Integrity

Every event sent to Wede has an `idempotency_key`. This guarantees that even if the same event is sent multiple times — due to network retries or reconnection — it is processed exactly once.

When connectivity is restored after an offline period, events are synchronised in order, with conflict resolution handled automatically.

## Security

All communication is encrypted in transit. Payloads are opaque to Wede — we transport your data but never read or store its contents. You control encryption at the application layer.

## Compliance

Wede is designed for regulated industries. Data residency is configurable per zone. All operations are logged in an immutable audit trail. Access is controlled by role-based permissions with full audit visibility.
